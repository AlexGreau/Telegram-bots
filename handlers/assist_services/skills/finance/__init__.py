"""Finance capability: a shared core engine with two faces.

- core.py   — pure filter/aggregate primitives (the engine)
- tools.py  — conversational log/search/aggregate tools for /assist (FINANCE_SKILL)
- report.py — deterministic build_report/format_report for the /report command
"""
